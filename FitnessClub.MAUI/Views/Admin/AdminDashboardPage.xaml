<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FitnessClub.MAUI.Views.Admin.AdminDashboardPage"
             Title="Admin Dashboard">

    <ContentPage.Content>
        <ScrollView>
            <VerticalStackLayout Spacing="20" Padding="20">

                <!-- Header -->
                <Label Text="👑 Admin Dashboard" 
                       FontSize="28" 
                       FontAttributes="Bold" 
                       HorizontalOptions="Center"
                       TextColor="Purple"/>

                <!-- Admin Cards -->
                <Frame CornerRadius="10" BackgroundColor="#E3F2FD" Padding="15">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Beheer Functionaliteiten" 
                               FontSize="18" 
                               FontAttributes="Bold"/>

                        <Button Text="📊 Statistieken" 
                                BackgroundColor="#4CAF50" 
                                TextColor="White"
                                HeightRequest="50"/>

                        <Button Text="👥 Gebruikers Beheer" 
                                BackgroundColor="#2196F3" 
                                TextColor="White"
                                HeightRequest="50"/>

                        <Button Text="📋 Lessen Beheer" 
                                BackgroundColor="#FF9800" 
                                TextColor="White"
                                HeightRequest="50"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- Quick Stats -->
                <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" 
                      ColumnSpacing="10" RowSpacing="10">

                    <Frame Grid.Column="0" Grid.Row="0" CornerRadius="10" Padding="15">
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Totaal Gebruikers" FontAttributes="Bold"/>
                            <Label Text="128" FontSize="24" FontAttributes="Bold" 
                                   HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </Frame>

                    <Frame Grid.Column="1" Grid.Row="0" CornerRadius="10" Padding="15">
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Actieve Lessen" FontAttributes="Bold"/>
                            <Label Text="24" FontSize="24" FontAttributes="Bold" 
                                   HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </Frame>

                    <Frame Grid.Column="0" Grid.Row="1" CornerRadius="10" Padding="15">
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Nieuwe Inschrijvingen" FontAttributes="Bold"/>
                            <Label Text="15" FontSize="24" FontAttributes="Bold" 
                                   HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </Frame>

                    <Frame Grid.Column="1" Grid.Row="1" CornerRadius="10" Padding="15">
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Omzet" FontAttributes="Bold"/>
                            <Label Text="€2.450" FontSize="24" FontAttributes="Bold" 
                                   HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </Frame>
                </Grid>

                <!-- Back Button -->
                <Button Text="⬅️ Terug naar Home" 
                        BackgroundColor="#9E9E9E" 
                        TextColor="White"
                        Clicked="OnBackClicked"/>

            </VerticalStackLayout>
        </ScrollView>
    </ContentPage.Content>

</ContentPage>