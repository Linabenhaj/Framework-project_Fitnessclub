<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FitnessClub.MAUI.Views.HomePage"
             Title="Mijn Fitness">

    <ScrollView>
        <VerticalStackLayout Spacing="25" Padding="20">

            <!-- Welkom -->
            <VerticalStackLayout Spacing="10" BackgroundColor="#2196F3" Padding="20">
                <Label Text="Welkom terug!" 
                       FontSize="22" 
                       TextColor="White"
                       FontAttributes="Bold"/>
                <Label Text="Je bent ingelogd als Fitness Lid" 
                       FontSize="14" 
                       TextColor="White"/>
            </VerticalStackLayout>

            <!-- Mijn gegevens -->
            <Label Text="📈 Mijn Voortgang" FontSize="18" FontAttributes="Bold"/>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="15">
                <VerticalStackLayout Grid.Column="0" Spacing="5" BackgroundColor="#4CAF50" Padding="15">
                    <Label Text="12" FontSize="24" TextColor="White" FontAttributes="Bold"/>
                    <Label Text="Workouts deze maand" FontSize="12" TextColor="White"/>
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Column="1" Spacing="5" BackgroundColor="#FF9800" Padding="15">
                    <Label Text="85%" FontSize="24" TextColor="White" FontAttributes="Bold"/>
                    <Label Text="Doelen behaald" FontSize="12" TextColor="White"/>
                </VerticalStackLayout>
            </Grid>

            <!-- Snel acties -->
            <Label Text="🚀 SNEL ACTIES" FontSize="18" FontAttributes="Bold"/>

            <Button Text="📚 Lessen Boeken" 
                    BackgroundColor="#2196F3"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="55"
                    Clicked="OnBookLessonsClicked"/>

            <Button Text="📅 Mijn Inschrijvingen" 
                    BackgroundColor="#009688"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="55"
                    Clicked="OnViewRegistrationsClicked"/>

            <Button Text="👤 Mijn Profiel" 
                    BackgroundColor="#9C27B0"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="55"
                    Clicked="OnViewProfileClicked"/>

            <Button Text="⚙️ Instellingen" 
                    BackgroundColor="#607D8B"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="55"
                    Clicked="OnSettingsClicked"/>

            <!-- Volgende les -->
            <VerticalStackLayout Spacing="8" BackgroundColor="#E8F5E9" Padding="15">
                <Label Text="▶️ VOLGENDE LES" FontSize="14" FontAttributes="Bold" TextColor="#2E7D32"/>
                <Label Text="Morgen 18:00 - HIIT Training" FontSize="16"/>
                <Label Text="Trainer: Jan Janssens" FontSize="14" TextColor="#666"/>
                <Button Text="📋 Details" 
                        BackgroundColor="#4CAF50"
                        TextColor="White"
                        CornerRadius="8"
                        HeightRequest="40"/>
            </VerticalStackLayout>

            <!-- Uitloggen -->
            <Button Text="🚪 Uitloggen" 
                    BackgroundColor="#D32F2F"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="55"
                    Margin="0,10,0,0"
                    Clicked="OnLogoutClicked"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>