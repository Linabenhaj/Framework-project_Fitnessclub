<UserControl x:Class="FitnessClub.WPF.Views.LessenOverzichtLid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header met contact info -->
        <Border Background="#f8f9fa" Padding="15" Margin="10" CornerRadius="5" BorderBrush="#dee2e6" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Beschikbare Lessen" 
                           FontSize="18" FontWeight="Bold" Margin="0,0,0,5" Foreground="#2c3e50"/>
                <TextBlock Text="Wil je je inschrijven voor een les? Neem contact op met de fitness manager:" 
                           FontStyle="Italic" Foreground="#666" FontSize="12" TextWrapping="Wrap"/>
                <TextBlock Text="📞 +32 458 57 57 57" 
                           FontSize="14" FontWeight="Bold" Foreground="#27ae60" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- DataGrid voor lessen (alleen lezen) -->
        <DataGrid Grid.Row="1" x:Name="LessenDataGrid" 
                  AutoGenerateColumns="False" Margin="10"
                  CanUserAddRows="False" IsReadOnly="True"
                  Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Les" Binding="{Binding Naam}" Width="120"/>
                <DataGridTextColumn Header="Beschrijving" Binding="{Binding Beschrijving}" Width="200"/>
                <DataGridTextColumn Header="Start Tijd" Binding="{Binding StartTijd, StringFormat='dd/MM/yyyy HH:mm'}" Width="120"/>
                <DataGridTextColumn Header="Eind Tijd" Binding="{Binding EindTijd, StringFormat='dd/MM/yyyy HH:mm'}" Width="120"/>
                <DataGridTextColumn Header="Max Deelnemers" Binding="{Binding MaxDeelnemers}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Refresh knop -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Vernieuwen" Click="RefreshClick" 
                    Background="#3498db" Foreground="White" 
                    Padding="15,8" Margin="5" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>