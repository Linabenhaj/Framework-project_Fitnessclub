<UserControl x:Class="FitnessClub.WPF.Views.AbonnementenOverzicht"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Abonnementen Beheer" FontSize="18" FontWeight="Bold" Margin="10"/>

        <DataGrid Grid.Row="1" x:Name="AbonnementenDataGrid" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Naam" Binding="{Binding Naam}" Width="150"/>
                <DataGridTextColumn Header="Prijs" Binding="{Binding Prijs, StringFormat=C}" Width="80"/>
                <DataGridTextColumn Header="Looptijd" Binding="{Binding LooptijdMaanden}" Width="80"/>
                <DataGridTextColumn Header="Omschrijving" Binding="{Binding Omschrijving}" Width="200"/>
                <DataGridTemplateColumn Header="Acties" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Bewerken" Click="BewerkenClick" 
                                        Tag="{Binding Id}" Margin="2" Padding="5"
                                        Background="#3498db" Foreground="White"/>
                                <Button Content="Verwijder" Click="VerwijderClick" 
                                        Tag="{Binding Id}" Margin="2" Padding="5"
                                        Background="#e74c3c" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
           
            <Button Content="Refresh" Click="RefreshClick" 
                    Background="#3498db" Foreground="White" Padding="10,5" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>