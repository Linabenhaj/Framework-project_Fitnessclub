<UserControl x:Class="FitnessClub.WPF.Views.InschrijvingenOverzicht"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Inschrijvingen Beheer" FontSize="18" FontWeight="Bold" Margin="10"/>

        <DataGrid Grid.Row="1" x:Name="InschrijvingenDataGrid" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Gebruiker" Binding="{Binding Gebruiker.Voornaam}" Width="120"/>
                <DataGridTextColumn Header="Les" Binding="{Binding Les.Naam}" Width="150"/>
                <DataGridTextColumn Header="Inschrijf Datum" Binding="{Binding InschrijfDatum, StringFormat=d}" Width="120"/>
                <DataGridTemplateColumn Header="Acties" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                
                                <Button Content="Verwijderen" Click="VerwijderenClick" Margin="5,0" Padding="10,2" Background="Red" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Nieuwe Inschrijving" Click="ToevoegenClick" 
                    Background="#27ae60" Foreground="White" 
                    Padding="15,8" Margin="5" FontWeight="Bold"/>
            <Button Content="Refresh" Click="RefreshClick" 
                    Background="#3498db" Foreground="White" 
                    Padding="15,8" Margin="5" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>