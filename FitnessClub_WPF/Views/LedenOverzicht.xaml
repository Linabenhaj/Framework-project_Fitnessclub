<UserControl x:Class="FitnessClub.WPF.Views.LedenOverzicht"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

       
        <TextBlock Grid.Row="0" Text="Leden Beheer" Style="{StaticResource HeaderStyle}"/>

     
        <DataGrid Grid.Row="1" x:Name="LedenDataGrid" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Voornaam" Binding="{Binding Voornaam}" Width="100"/>
                <DataGridTextColumn Header="Achternaam" Binding="{Binding Achternaam}" Width="100"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                <DataGridTextColumn Header="Telefoon" Binding="{Binding Telefoon}" Width="100"/>
                <DataGridTemplateColumn Header="Acties" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Bewerken" Click="BewerkenClick" 
                                        Tag="{Binding Id}" Margin="2" Padding="5"/>
                                <Button Content="Verwijder" Click="VerwijderClick" 
                                        Tag="{Binding Id}" Margin="2" Padding="5"
                                        Background="#e74c3c" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

   
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Nieuw Lid" Click="ToevoegenClick" Style="{StaticResource SuccessButtonStyle}"/>
            <Button Content="Refresh" Click="RefreshClick" Style="{StaticResource ButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>