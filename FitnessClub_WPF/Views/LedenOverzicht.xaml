<UserControl x:Class="FitnessClub.WPF.Views.LedenOverzicht"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Leden Beheer" FontSize="18" FontWeight="Bold" Margin="10"/>

        <DataGrid Grid.Row="1" x:Name="LedenDataGrid" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Voornaam" Binding="{Binding Voornaam}" Width="100"/>
                <DataGridTextColumn Header="Achternaam" Binding="{Binding Achternaam}" Width="100"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                <DataGridTextColumn Header="Telefoon" Binding="{Binding Telefoon}" Width="100"/>
                <DataGridTextColumn Header="Abonnement" Binding="{Binding Abonnement.Naam}" Width="120"/>
                <DataGridTemplateColumn Header="Acties" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <!-- BEWERKEN Button extra -->
                                <Button Content="Bewerken" Click="BewerkenClick" 
                                        Tag="{Binding Id}" Margin="2" Padding="5"
                                        Background="#3498db" Foreground="White"/>

                                
                                <Button Content="Verwijder" Click="VerwijderClick" 
                                        Tag="{Binding Id}" Margin="2" Padding="5"
                                        Background="#e74c3c" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Refresh" Click="RefreshClick" 
                    Background="#3498db" Foreground="White" Padding="10,5" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>