<UserControl x:Class="FitnessClub.WPF.Views.MijnAbonnement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Mijn Abonnement" 
                       FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- ZELFDE STIJL ALS REGISTRATIE -->
            <Border Background="#f8f9fa" Padding="20" Margin="0,0,0,15" CornerRadius="5" BorderBrush="#dee2e6" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Uw huidige abonnement:" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" Foreground="#2c3e50"/>

                    <StackPanel>
                        <TextBlock x:Name="AbonnementNaamText" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" Foreground="#2c3e50"/>
                        <TextBlock x:Name="AbonnementPrijsText" FontSize="14" Margin="0,0,0,5" Foreground="#27ae60"/>
                        <TextBlock x:Name="AbonnementOmschrijvingText" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="#666"/>
                        <TextBlock x:Name="AbonnementLooptijdText" FontSize="12" FontStyle="Italic" Foreground="#999"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <TextBlock Text="Uw abonnement is gekozen tijdens registratie en kan niet worden gewijzigd via deze applicatie." 
                       FontStyle="Italic" Foreground="#666" Margin="0,20,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>