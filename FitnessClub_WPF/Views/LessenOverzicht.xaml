<UserControl x:Class="FitnessClub.WPF.Views.LessenOverzicht"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

       
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Lessen Beheer" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Les Toevoegen" Click="ToevoegenClick" 
                    Background="#27ae60" Foreground="White" 
                    Padding="15,8" Margin="20,0,0,0" FontWeight="Bold"/>
        </StackPanel>

       
        <DataGrid Grid.Row="1" x:Name="LessenDataGrid" 
                  AutoGenerateColumns="False" Margin="10"
                  CanUserAddRows="False" IsReadOnly="True"
                  Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Les" Binding="{Binding Naam}" Width="120"/>
                <DataGridTextColumn Header="Beschrijving" Binding="{Binding Beschrijving}" Width="200"/>
                <DataGridTextColumn Header="Start Tijd" Binding="{Binding StartTijd, StringFormat='dd/MM/yyyy HH:mm'}" Width="120"/>
                <DataGridTextColumn Header="Eind Tijd" Binding="{Binding EindTijd, StringFormat='dd/MM/yyyy HH:mm'}" Width="120"/>
                <DataGridTextColumn Header="Max Deelnemers" Binding="{Binding MaxDeelnemers}" Width="100"/>
                <DataGridTemplateColumn Header="Acties" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                
                                <Button Content="Verwijderen" Click="VerwijderenClick" 
                                        Margin="5,0" Padding="10,2" 
                                        Background="#e74c3c" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

     
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Vernieuwen" Click="RefreshClick" 
                    Background="#3498db" Foreground="White" 
                    Padding="15,8" Margin="5" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>